# Kora devnet Ansible inventory
#
# Copy this file to hosts.yml and fill in your values:
#   cp hosts.yml.example hosts.yml
#
# Requirements:
#   - Target must be Arch Linux (playbooks use pacman)
#   - SSH key-based authentication
#   - hosts.yml is gitignored to protect server IPs
#
# Security: use a dedicated deploy user with limited sudo instead of root.
# See provision.yml for setting up the kora-deploy user.
all:
  children:
    devnet:
      hosts:
        hetzner-devnet:
          ansible_host: YOUR_SERVER_IP
          ansible_user: kora-deploy
          ansible_python_interpreter: /usr/bin/python3
